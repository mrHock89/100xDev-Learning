// Write A Program to reverse all the elements of an array.

const my_arry = [1, 3, 4, 5, 3, 6];
let arrayLength = my_arry.length;
for(let i = arrayLength; i >= 0; i--) {
    console.log(my_arry[i]);
}